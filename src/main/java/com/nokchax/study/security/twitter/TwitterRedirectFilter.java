package com.nokchax.study.security.twitter;

// twitter 로그인에 필요한 accessToken 과 tokenSecret 을 얻기 위해서 트위터 쪽으로 요청을 보내기 위한 필터
public class TwitterRedirectFilter {
    // twitter redirect 시에 callback url 을 설정해서 요청 보낼 수 있다.
    // RequestToken 은 저장해 둬야 한다.
}
